session: Quiqr Services

root: ~/cQuiqr

before_start:

stop:

windows:
  - name: board
    root: ~/cQuiqr/quiqr-board
    layout: main-vertical
    commands:
      - echo "hi"
    panes:
      - type: horizontal
        commands:
          - ./pogocli
      - type: horizontal
        commands:
      - nix-shell
          - ./sentry --build="make build_cli" --run="ls"
      - type: horizontal
        commands:
          - ./sentry

  - name: pay
    root: ~/cQuiqr/quiqr-pay
    layout: main-vertical
    commands:
     - echo "hi"
    panes:
      - type: horizontal
        commands:
          - ./rerun.sh
      - type: horizontal
        commands:
          - stripe listen --forward-to localhost:4242/webhook

  - name: reddis
    root: ~/cQuiqr/redis-server
    layout: main-vertical
    commands:
     - echo "hi"
    panes:
      - type: horizontal
        commands:
          - ./rerun.sh

